{
    "ssh": {
        "host": "ssh_host_address",
        "port": 22,
        "user": "ssh_user",
        "password": "ssh_password"
    },
    "database": {
        "host": "localhost",
        "port": 3306,
        "user": "db_user",
        "password": "db_password",
        "name": "db_name"
    }
}

